services:
  - type: web
    name: edusync-backend
    env: python
    rootDir: edusync-backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
